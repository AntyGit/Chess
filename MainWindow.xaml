<Window x:Name="window" x:Class="Chess.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Width="1280" Height="720">

    <Grid Name="grid" Background="Black">
        <UniformGrid Name="tile_grid" Rows="8" Columns="8" Margin="0,25,0,0"/>
        <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top" Width="{Binding ActualWidth, ElementName=window, Mode=OneWay}" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" Height="{Binding ActualHeight, ElementName=ResetButton, Mode=OneWay}">
            <Button x:Name="ResetButton" Content="Reset&#xA;" Margin="0,0,1195,0" Click="ResetButton_Click" Height="25" VerticalAlignment="Center"/>
        </StackPanel>

    </Grid>
</Window>
